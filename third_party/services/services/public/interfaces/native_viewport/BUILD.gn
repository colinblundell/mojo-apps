# Copyright 2014 The Chromium Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/config/mojo.gni")

import(rebase_path("mojo/public/tools/bindings/mojom.gni", ".", mojo_root))

# GYP version: mojo/mojo_services.gypi:mojo_native_viewport_bindings
mojom("native_viewport") {
  sources = [
    "native_viewport.mojom",
  ]

  public_deps = [
    rebase_path("mojo/services/public/interfaces/geometry", ".", mojo_root),
    rebase_path("mojo/services/public/interfaces/gpu", ".", mojo_root),
    rebase_path("mojo/services/public/interfaces/input_events", ".", mojo_root),
    rebase_path("mojo/services/public/interfaces/surfaces:surface_id", ".", mojo_root),
  ]
}

# Copyright 2014 The Chromium Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/config/mojo.gni")

source_set("surfaces") {
  public_configs = [ rebase_path("mojo/build/config", ".", mojo_root) + ":mojo_sdk" ]
  sources = [
    "surfaces_utils.cc",
    "surfaces_utils.h",
  ]

  deps = [
    rebase_path("mojo/services/public/interfaces/geometry", ".", mojo_root),
    rebase_path("mojo/services/public/interfaces/surfaces", ".", mojo_root),
  ]
}
